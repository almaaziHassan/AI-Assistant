# ============================================
# AI Virtual Receptionist - Backend Configuration
# ============================================
# Copy this file to .env and fill in the values

# ===================
# REQUIRED VARIABLES
# ===================

# Groq AI API Key (get from https://console.groq.com)
GROQ_API_KEY=your_groq_api_key_here

# Admin Panel Password (change this in production!)
ADMIN_PASSWORD=your_secure_password_here

# ===================
# OPTIONAL VARIABLES
# ===================

# Server Configuration
PORT=3000
NODE_ENV=production

# Groq Model (default: llama-3.3-70b-versatile)
GROQ_MODEL=llama-3.3-70b-versatile

# Email Configuration
# Option 1: Brevo API (Recommended - no port blocking issues)
BREVO_API_KEY=your_brevo_api_key_here
BREVO_FROM_EMAIL=your_verified_email@example.com
BREVO_FROM_NAME=Your Business Name

# Option 2: SMTP (May be blocked on some hosts like Railway)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_email_password
# SMTP_FROM=noreply@yourbusiness.com

# Frontend URL (for CORS)
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Database Configuration
# Option 1: PostgreSQL (Supabase - Recommended for production)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres

# Option 2: SQLite (Local development - used if DATABASE_URL not set)
# No configuration needed - will use ./data/receptionist.db

# ===================
# USER AUTHENTICATION
# ===================

# JWT Secret Key (generate with: openssl rand -base64 32)
JWT_SECRET=your_secure_random_jwt_secret_here

# Google OAuth (get from https://console.cloud.google.com)
# 1. Create a project at Google Cloud Console
# 2. Enable "Google+ API" or "Google People API"
# 3. Go to "Credentials" > "Create Credentials" > "OAuth client ID"
# 4. Set authorized redirect URI: http://localhost:3000/api/user-auth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Backend URL (needed for OAuth callback)
BACKEND_URL=http://localhost:3000

